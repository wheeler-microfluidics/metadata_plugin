# source will be downloaded prior to filling in jinja templates
# Example assumes that this folder has setup.py in it
source:
  git_url: ../

package:
  name: metadata_plugin
{% if GIT_DESCRIBE_NUMBER > '0' %}
  version: {{ GIT_DESCRIBE_TAG[1:] }}.post{{ GIT_DESCRIBE_NUMBER }}
{% else %}
  version: {{ GIT_DESCRIBE_TAG[1:] }}
{% endif %}

build:
  number: 0
  script: python -m mpm.bin.build

requirements:
  build:
    - barcode-scanner
    - flatland-fork
    - jsonschema
    - microdrop-plugin-template
    - microdrop-utility
    - microdrop >=2.0.post49.dev258705497
    - microdrop-plugin-manager >=0.11
    - pandas
    - pyyaml
    - path_helpers >=0.2.post4
    - redirect-io
    - wheeler.pygtkhelpers

  run:
    - barcode-scanner
    - flatland-fork
    - jsonschema
    - microdrop-plugin-template
    - microdrop-utility
    - microdrop >=2.0.post49.dev258705497
    - microdrop-plugin-manager >=0.11
    - pandas
    - pyyaml
    - path_helpers >=0.2.post4
    - redirect-io
    - wheeler.pygtkhelpers

test:
  commands:
    - python -c "import importlib; import mpm.api; import os; import sys; sys.path.insert(0, mpm.api.MICRODROP_CONDA_SHARE.joinpath('plugins', 'available')); importlib.import_module(os.environ.get('PKG_NAME'))"

about:
  home: http://github.com/wheeler-microfluidics/metadata-plugin
  license: GPL
